apiVersion: apps/v1
kind: Deployment
metadata:
  name: roster-svc
  labels:
    app: roster-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: roster-svc
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: roster-svc
    spec:
      containers:
        - name: roster-svc
          image: roster-svc:dev
          imagePullPolicy: Never
          ports:
            - containerPort: 4002
          env:
            - name: PORT
              value: "4002"
            - name: SUPABASE_URL
              valueFrom:
                secretKeyRef:
                  name: roster-svc-secrets
                  key: SUPABASE_URL
            - name: SUPABASE_ANON_KEY
              valueFrom:
                secretKeyRef:
                  name: roster-svc-secrets
                  key: SUPABASE_ANON_KEY
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 4002
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 4002
            initialDelaySeconds: 5
            periodSeconds: 10
