# Template only — never commit real values. Create the actual secret with:
#
#   kubectl create secret generic frontend-secrets \
#     --from-literal=NEXT_PUBLIC_SUPABASE_URL='https://<project-id>.supabase.co' \
#     --from-literal=NEXT_PUBLIC_SUPABASE_ANON_KEY='<anon-key>'
apiVersion: v1
kind: Secret
metadata:
  name: frontend-secrets
type: Opaque
stringData:
  NEXT_PUBLIC_SUPABASE_URL: "REPLACE_ME"
  NEXT_PUBLIC_SUPABASE_ANON_KEY: "REPLACE_ME"
